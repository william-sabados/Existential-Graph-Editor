// This is the home for all the conditions which enable and disable certain buttons.
function egButtons()
{

// SPECIALIZE
if(model.returnByID(getSelectionEgId, 1).isNegative = true)
{
    // Yes. Can add.
}
// GENERALIZE
if(model.returnByID(getSelectionEgId, 1).isNegative = false)
{
    // Yes. Can remove.
}
// COPY

// REMOVE

// You can never NOT add a double negation.
// DOUBLE NEST

// REMOVE DOUBLE NEST

}